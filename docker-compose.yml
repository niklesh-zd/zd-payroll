version: '3'

services:
  app-dev:
    image: rohitkas/app-prod:latest
    container_name: app-prod
    restart: always
    ports:
      - "80:7071"
    environment:
      - MONGODB_URI=mongodb://mongodb:27017/employees
    depends_on:
      - mongodb

  mongodb:
    image: mongo:latest
    restart: always
    volumes:
      - mongodb-data:/data/db


volumes:
